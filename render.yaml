services:
  - type: web
    name: calliq-backend
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn project.app:app --host 0.0.0.0 --port $PORT
